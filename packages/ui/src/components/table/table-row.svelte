<script lang="ts">
	import { cn, type WithElementRef } from "@DashboardPT2/ui/lib/utils.js";
	import type { HTMLAttributes } from "svelte/elements";

	let {
		ref = $bindable(null),
		class: className,
		children,
		...restProps
	}: WithElementRef<HTMLAttributes<HTMLTableRowElement>> = $props();
</script>

<tr bind:this={ref} data-slot="table-row" class={cn("border-b transition-colors has-[[aria-haspopup=menu][aria-expanded=true]]:bg-[color-mix(in_oklab,var(--card),var(--muted)_50%)] data-[state=selected]:bg-muted", className)} {...restProps}>
	{@render children?.()}
</tr>
