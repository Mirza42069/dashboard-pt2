{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "services": {
    "web": {
      "root": "apps/web",
      "framework": "sveltekit-1",
      "installCommand": "cd ../.. && bun install",
      "buildCommand": "cd ../.. && bun run db:migrate:deploy && bun run --cwd apps/web build"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": {
        "service": "web"
      }
    }
  ]
}
